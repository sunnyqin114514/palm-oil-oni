# DeepSeek 产量解读配置（可选）。
# 复制本文件为 .env 并填入真实 Key 后，/predict-explain 会调用 DeepSeek 生成中文解读。
# 不配置时，后端自动降级为本地规则解读，网站照常可用。

DEEPSEEK_API_KEY=
# 以下可选，默认即可
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_TIMEOUT=30
